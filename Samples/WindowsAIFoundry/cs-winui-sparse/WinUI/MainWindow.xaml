<Window
    x:Class="WindowsAISample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsAISample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="12">
            <TextBlock Text="Windows AI Sample (Sparse WinUI)" FontSize="24" FontWeight="Bold" Margin="0,0,0,12"/>
            <TextBlock Text="This is a minimal sample showing how to use Windows AI APIs with WinUI in a sparse package." 
                       TextWrapping="Wrap" Margin="0,0,0,12"/>
            <Button x:Name="TestIdentityButton" Content="Test Package Identity" Click="TestIdentityButton_Click" Margin="0,0,0,12"/>
            <TextBlock x:Name="IdentityStatusTextBlock" Text="Package Identity Status: Unknown" Margin="0,0,0,12"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="{ThemeResource ButtonBorderThemeBrush}" BorderThickness="1" Margin="12">
            <Grid>
                <TextBlock Text="Identity-based API features would go here" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           FontStyle="Italic"/>
            </Grid>
        </Border>
    </Grid>
</Window>